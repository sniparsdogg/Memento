const SECCAO_FOTOS = "#galeriaFotos"

function appendPhotos(arrayFotos){
    let posicao = 0;
    for(i in addedFiles){
        for(tags, src in i){
            console.log(tags, src);
        }
    }
}

window.addEventListener("load", principal);

function principal(){
    appendPhotos(addedFiles);
}